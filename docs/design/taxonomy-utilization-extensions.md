# 3層分類体系（Taxonomy）の拡張 設計

## まだ未実装

### 1. 命名プロンプトへのカテゴリ文脈注入

文書上の案はあるが、現状の Go バッチ命名ロジック（`name.go`）に taxonomy 文脈を明示的に渡して処理する部分は未実装。

### 2. カテゴリ別スタンス集計API

分類を用いた長期的・メディア別のバイアス集計・確認機能は未実装。

- `/api/bias/stance`
- `source × category` 集計
- 媒体傾向のカテゴリ別マップ

### 3. カバレッジマトリクスのカテゴリ絞り込み

マトリクス表示へのカテゴリ・フィルタリング。
`CoverageMatrix` は現状、カテゴリフィルタなしで全グループを扱う仕様となっている。
