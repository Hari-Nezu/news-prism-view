# 媒体バイアス・スタンス分析 設計

## 概要
媒体対象定義は `src/lib/config/feed-configs.ts` に実装済みだが、以下は将来構想として未実装。

## まだやっていないこと

- 媒体の政治的立ち位置を静的メタデータとして付与すること
- `media-registry.ts` のような独立レジストリ化
- 媒体別の長期平均スタンス集計API (`/api/bias/stance`)

現状の方針は、**立ち位置を事前に決め打ちしない** こと。
記事単位の分析結果や、どのトピックを報じたかから後で集計する。

## 実装案: スタンス集計 API

- `/api/bias/stance` を追加し、`source × category` の集計を返す。
- CoverageMatrix またはランキング画面で category 絞り込みを追加する。
- taxonomy 文脈を使った naming 改善の足場を作る。
- 静的な「媒体の思想ラベル付け」に依存しない設計とする。
